name: build

on:
  pull_request:
  push:
    branches:
      - "development"
      - "main"

env:
  GO_VERSION: '1.21.3'

jobs:
  test:
    name: Run test
    runs-on: ubuntu-latest
    env:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v3
      - name: test
        if: ${{ github.event_name == 'push'}}
        run: make vendor && make test
      - name: test with coverage
        if: ${{ github.event_name == 'pull_request'}}
        run: make vendor && make test-coverage
      - name: upload coverage to codecov.io
        if: ${{ github.event_name == 'pull_request'}}
        run: bash <(curl -s https://codecov.io/bash)
  linux32:
    name: Check build linux-386 binary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Check build
        run: make vendor && make build-linux32 VERSION=$GITHUB_REF_NAME
  linux64:
    name: Check build linux-amd64 binary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Check build
        run: make vendor && make build-linux64 VERSION=$GITHUB_REF_NAME
  win32:
    name: Check build win32 binary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Check build
        run: make vendor && make build-win32 VERSION=$GITHUB_REF_NAME
  win64:
    name: Check build win64 binary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Check build
        run: make vendor && make build-win64 VERSION=$GITHUB_REF_NAME
  macos:
    name: Check build macos binary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: ${{ env.GO_VERSION }}
      - name: Check build
        run: make vendor && make build-mac VERSION=$GITHUB_REF_NAME
